import { showSumItems } from './loop-sum'
import { showRecursiveSummedArray } from './recursive-sum'
import { showRecursiveCount } from './recursive-count'
import { showRecursiveMax } from './recursive-max'
import { showQuicksort } from './quicksort'

showSumItems([1, 2, 3, 4, 5])
showRecursiveSummedArray([1, 2, 3, 4, 5])
showRecursiveCount([1, 2, 3, 4, 5])
showRecursiveMax([1, 2, 3, 4, 5])
showQuicksort([1, 2])
